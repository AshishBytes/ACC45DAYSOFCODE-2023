#include <iostream>
#include <cmath>

using namespace std;

int main() {
    int T;
    cin >> T;

    while (T--) {
        int X, Y;
        cin >> X >> Y;

        int diff = abs(X - Y);
        cout << diff << endl;
    }

    return 0;
}
